# Wersja 1.3.0 - Intelligent Context & Streaming

Wydanie to wprowadza znaczące ulepszenia w zakresie interakcji z użytkownikiem (streaming) oraz inteligencji kontekstowej (RAG), a także podnosi standardy jakości kodu (QA).

## Główne Funkcje

### 🚀 Płynne Strumieniowanie (Streaming Output)
Odpowiedzi modelu pojawiają się teraz słowo po słowie, dając natychmiastową informację zwrotną.
- **Bez artefaktów**: Nowy silnik renderowania (`StreamMarkdownRenderer`) buforuje znaki, zapobiegając "mruganiu" kolorów ANSI czy łamaniu składni Markdown.
- **Kompatybilność**: Działa z wszystkimi dostawcami wspierającymi strumieniowanie (OpenAI, Sherlock, OpenWebUI/Ollama).

### 🧠 Lightweight RAG (Retrieval-Augmented Generation)
Aiferro zyskał pamięć krótkotrwałą dla Twoich plików.
- **Flaga `--rag`**: Automatycznie przeszukuje pliki w katalogu, by znaleźć fragmenty kodu pasujące do pytania.
- **Flaga `-d / --directory`**: Pozwala wskazać katalog źródłowy (domyślnie obecny).
- **Smart Chunking**: Inteligentne dzielenie plików na funkcje i klasy Python oraz sekcje Markdown.
- **Wydajność**: Autorski silnik oparty na TF-IDF/BM25, bez ciężkich zależności.

### ✅ Quality Assurance
- **TDD**: Kluczowe moduły (RAG, Output) rozwijane w podejściu Test-Driven Development.
- **Strict Typing**: Kod spełnia rygorystyczne wymogi `mypy --strict`.
- **Pełne Testy**: Ponad 110 testów jednostkowych weryfikuje stabilność systemu. Stworzono skrypt `./run_tests.sh` dla łatwej weryfikacji.

## Zmiany Techniczne
- Dodano `aifr/rag.py` z implementacją silnika wyszukiwania.
- Zaktualizowano `aifr/cli.py` i `aifr/output.py` pod kątem obsługi generatorów (strumieniowania).
- Usunięto przestarzały `cli_old.py`.
- Zaktualizowano konfigurację `mypy` i testy `pytest`.
